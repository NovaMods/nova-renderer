diff --git "a/./src/main/java/net/minecraft/client/Minecraft.java" "b/../Nova Renderer/src/main/java/net/minecraft/client/Minecraft.java"
index ad84c84..addb1d3 100644
--- "a/./src/main/java/net/minecraft/client/Minecraft.java"
+++ "b/../Nova Renderer/src/main/java/net/minecraft/client/Minecraft.java"
@@ -1,5 +1,6 @@
 package net.minecraft.client;
 
+import com.continuum.nova.NovaRenderer;
 import com.google.common.collect.Lists;
 import com.google.common.collect.Queues;
 import com.google.common.collect.Sets;
@@ -383,6 +384,8 @@ public class Minecraft implements IThreadListener, ISnooperInfo
     /** Profiler currently displayed in the debug screen pie chart */
     private String debugProfilerName = "root";
 
+    private NovaRenderer nova = new NovaRenderer();
+
     public Minecraft(GameConfiguration gameConfig)
     {
         theMinecraft = this;
@@ -408,6 +411,8 @@ public class Minecraft implements IThreadListener, ISnooperInfo
         this.jvm64bit = isJvm64bit();
         this.theIntegratedServer = null;
 
+        nova.preInit();
+
         if (gameConfig.serverInfo.serverName != null)
         {
             this.serverName = gameConfig.serverInfo.serverName;
@@ -538,6 +543,7 @@ public class Minecraft implements IThreadListener, ISnooperInfo
         this.mcResourceManager.registerReloadListener(this.standardGalacticFontRenderer);
         this.mcResourceManager.registerReloadListener(new GrassColorReloadListener());
         this.mcResourceManager.registerReloadListener(new FoliageColorReloadListener());
+        mcResourceManager.registerReloadListener(nova);
         AchievementList.OPEN_INVENTORY.setStatStringFormatter(new IStatStringFormat()
         {
             public String formatString(String str)
@@ -1037,6 +1043,7 @@ public class Minecraft implements IThreadListener, ISnooperInfo
             int j = scaledresolution.getScaledHeight();
             ((GuiScreen)guiScreenIn).setWorldAndResolution(this, i, j);
             this.skipRenderWorld = false;
+            nova.setGuiScreen(guiScreenIn);
         }
         else
         {
@@ -1156,6 +1163,7 @@ public class Minecraft implements IThreadListener, ISnooperInfo
         {
             this.mcProfiler.endStartSection("gameRenderer");
             this.entityRenderer.updateCameraAndRender(this.timer.renderPartialTicks, i);
+            nova.updateCameraAndRender(timer.renderPartialTicks, i, this);
             this.mcProfiler.endSection();
         }
  
@@ -1881,6 +1889,7 @@ public class Minecraft implements IThreadListener, ISnooperInfo
             if (!this.isGamePaused)
             {
                 this.entityRenderer.updateRenderer();
+                nova.updateRenderer();
             }
 
             this.mcProfiler.endStartSection("levelRenderer");
 
