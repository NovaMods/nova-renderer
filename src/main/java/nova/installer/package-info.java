/**
 * Package that holds all the installer code
 *
 * <p>The installer runs through all the code in the Minecraft jar and transforms the classes as needed. "As needed"
 * means "If I have a transformer defined already"</p>
 */
package com.continuum.nova.installer;