/*!
 * \brief
 *
 * \author ddubois 
 * \date 19-Sep-16.
 */

#include "renderer.h"

namespace nova {
    namespace view {
        renderer::renderer(adapter::adapter_facade& adapter) : adapter(adapter) {

        }

        void renderer::render_frame() {

        }
    }
}
