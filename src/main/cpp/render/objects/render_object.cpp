/*!
 * \brief
 *
 * \author ddubois 
 * \date 02-Feb-17.
 */

#include "render_object.h"

namespace nova {
    /*render_object::render_object(render_object&& other) {
        is_solid = other.is_solid;
        is_transparent = other.is_transparent;
        is_cutout = other.is_cutout;
        is_emissive = other.is_emissive;
        damage_level = other.damage_level;

        name = std::move(other.name);
        geometry = std::move(other.geometry);
        color_texture = other.color_texture;
        normalmap = std::move(other.normalmap);
        data_texture = std::move(other.data_texture);
    }*/
}
